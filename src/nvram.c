#include <stdint.h>
#include <stddef.h>
#include <string.h>
#include <endian.h>
#include "nvram.h"


/* CRC table in Netgear Firmware */
static const uint8_t crc8_table[256] = {
	
};


/* Calculate the CRC8 of the provided buffer
 * buffer:		The input buffer
 * buffer_len:	The input buffer length
 * crc:			CRC starting value (for small chunks of buffer data)
 * NOTE: This function has been reverse engineered from Netgear's firmware
 */
static uint8_t hndcrc8 (uint8_t* buffer, size_t buffer_len, uint8_t crc)
{
	uint8_t buffer_b;
	uint8_t crc_i;
	size_t i;

	for (i = 0; i < buffer_len; i++)
	{
		buffer_b = buffer[i];
		crc_i = buffer_b ^ crc;
		crc = crc8_table[crc_i];
	}

	return crc;
}


/* Get the NVRAM magic
 * buffer:		The NVRAM buffer
 * RETURN:		The NVRAM magic number
 */
uint32_t get_magic (uint8_t* buffer)
{
	if (!buffer)
		return 0;

	return be32toh (*((uint32_t *)(buffer + NVRAM_INDEX_MAGIC)));
}


/* Applies to a NVRAM image the provided magic number
 * buffer:		The NVRAM buffer
 * magic:		The magic number calculated before
 */
void set_magic (uint8_t* buffer, uint32_t magic)
{
	uint32_t magic_be;

	if (!buffer)
		return;

	magic_be = htobe32 (magic);
	memcpy (buffer + NVRAM_INDEX_MAGIC, &magic_be, NVRAM_SIZE_MAGIC);
}



/* Get the NVRAM length
 * buffer:		The NVRAM buffer
 * RETURN:		The NVRAM length
 */
uint32_t get_length (uint8_t* buffer)
{
	if (!buffer)
		return 0;

	return (*((uint32_t *)(buffer + NVRAM_INDEX_LENGTH)));
}


/* Applies to a NVRAM image the provided length
 * buffer:		The NVRAM buffer
 * length:		The image length
 */
void set_length (uint8_t* buffer, uint32_t length)
{
	if (!buffer)
		return;

	memcpy (buffer + NVRAM_INDEX_LENGTH, &length, NVRAM_SIZE_LENGTH);
}


/* Get the NVRAM CRC8
 * buffer:		The NVRAM buffer
 * RETURN:		The NVRAM CRC8
 */
uint8_t get_crc (uint8_t* buffer)
{
	if (!buffer)
		return 0;

	return (*(buffer + NVRAM_INDEX_CRC));
}


/* Applies to a NVRAM image the provided CRC8
 * buffer:		The NVRAM buffer
 * crc:			The image CRC8
 */
void set_crc (uint8_t* buffer, uint8_t crc)
{
	if (!buffer)
		return;

	buffer[NVRAM_INDEX_CRC] = crc;
}


/* Calculate the NVRAM checksum
 * buffer:		The NVRAM buffer
 * RETURN:		The NVRAM calculated CRC8
 * NOTE: This function has been reverse engineered from Netgear's firmware
 */
uint8_t calculate_crc (uint8_t* buffer)
{
	uint8_t crc;

	/* Calculate the fields first */
	crc = hndcrc8(buffer + NVRAM_INDEX_FIELD1, NVRAM_SIZE_FIELD1, NVRAM_CRC_START);
	crc = hndcrc8(buffer + NVRAM_INDEX_FIELD2, NVRAM_SIZE_FIELD2, crc);

	/* Calculate for the data */
	crc = hndcrc8(buffer + NVRAM_INDEX_DATA, get_length(buffer) - NVRAM_INDEX_DATA, crc);

	return crc;
}

/* Applies to a NVRAM image the FIELD1 content
 * buffer:		The NVRAM buffer
 */
void set_field1 (uint8_t* buffer)
{
	memset (buffer + NVRAM_INDEX_FIELD1, NVRAM_CONTENT_FIELD1, NVRAM_SIZE_FIELD1);
}


/* Applies to a NVRAM image the FIELD2 content
 * buffer:		The NVRAM buffer
 */
void set_field2 (uint8_t* buffer)
{
	memset (buffer + NVRAM_INDEX_FIELD2, NVRAM_CONTENT_FIELD2, NVRAM_SIZE_FIELD2);
}
